<div class="master-container">
  <div class="form-container">
    <%= form_with(model: crop_mix, local: true) do |form| %>
      <% if crop_mix.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(crop_mix.errors.count, "error") %> prohibited this crop_mix from being saved:</h2>

          <ul>
          <% crop_mix.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= form.label :name %>
        <%= form.text_field :name, id: :crop_mix_name, class: "form-control" %>
      </div>

      <br><hr><br>

      <div class="form-group">
        <%= form.label :crop_one_id %>
        <%= form.select :crop_one_id, Crop.all.order(:crop).collect{|crop| ["#{crop.crop} - #{crop.crop_variety} (#{crop.crop_type})", crop.id ]}, include_blank: true, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_one_parts %>
        <%= form.number_field :crop_one_parts, id: :crop_mix_crop_one_parts, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_two_id %>
        <%= form.select :crop_two_id, Crop.all.order(:crop).collect{|crop| ["#{crop.crop} - #{crop.crop_variety} (#{crop.crop_type})", crop.id ]}, include_blank: true, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_two_parts %>
        <%= form.number_field :crop_two_parts, id: :crop_mix_crop_two_parts, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_three_id %>
        <%= form.select :crop_three_id, Crop.all.order(:crop).collect{|crop| ["#{crop.crop} - #{crop.crop_variety} (#{crop.crop_type})", crop.id ]}, include_blank: true, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_three_parts %>
        <%= form.number_field :crop_three_parts, id: :crop_mix_crop_three_parts, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_four_id %>
        <%= form.select :crop_four_id, Crop.all.order(:crop).collect{|crop| ["#{crop.crop} - #{crop.crop_variety} (#{crop.crop_type})", crop.id ]}, include_blank: true, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_four_parts %>
        <%= form.number_field :crop_four_parts, id: :crop_mix_crop_four_parts, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_five_id %>
        <%= form.select :crop_five_id, Crop.all.order(:crop).collect{|crop| ["#{crop.crop} - #{crop.crop_variety} (#{crop.crop_type})", crop.id ]}, include_blank: true, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_five_parts %>
        <%= form.number_field :crop_five_parts, id: :crop_mix_crop_five_parts, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_six_id %>
        <%= form.select :crop_six_id, Crop.all.order(:crop).collect{|crop| ["#{crop.crop} - #{crop.crop_variety} (#{crop.crop_type})", crop.id ]}, include_blank: true, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_six_parts %>
        <%= form.number_field :crop_six_parts, id: :crop_mix_crop_six_parts, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_seven_id %>
        <%= form.select :crop_seven_id, Crop.all.order(:crop).collect{|crop| ["#{crop.crop} - #{crop.crop_variety} (#{crop.crop_type})", crop.id ]}, include_blank: true, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_seven_parts %>
        <%= form.number_field :crop_seven_parts, id: :crop_mix_crop_seven_parts, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_eight_id %>
        <%= form.select :crop_eight_id, Crop.all.order(:crop).collect{|crop| ["#{crop.crop} - #{crop.crop_variety} (#{crop.crop_type})", crop.id ]}, include_blank: true, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :crop_eight_parts %>
        <%= form.number_field :crop_eight_parts, id: :crop_mix_crop_eight_parts, class: "form-control" %>
      </div>

      <div class="actions" align="center">
        <%= form.submit "submit", class: "btn btn-primary btn-lg" %>
      </div>
    <% end %>    
  </div>
</div>

