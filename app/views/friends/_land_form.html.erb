<%= form_with(model: @friend, local: true) do |form| %>
  <% if @friend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friend.errors.count, "error") %> prohibited this friend from being saved:</h2>

      <ul>
      <% friend.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :name, id: :friend_name, placeholder: " Name", class: "form-control signup"  %>
  </div>

  <div class="form-group">
    <%= form.text_field :email, id: :friend_email, placeholder: " Email", class: "form-control signup"  %>
  </div>

  <p>
    <i>we'll never share your info with anyone</i>
  </p>

  <div class="actions">
    <%= form.submit "Join X-Street", class: "btn btn-primary btn-lg" %>
  </div>
<% end %>
