<h6 class="dropdown-header">END SOAKS</h6>
<% @drains_needed.each{|treatment| %>
	<%= link_to "#{treatment.seed_quantity_oz} oz #{Crop.where(id: treatment.crop_id)[0].crop.capitalize}", seed_treatments_end_soak_path(seed_treatment: treatment) %>
<% } %>
<div class="dropdown-divider"></div>