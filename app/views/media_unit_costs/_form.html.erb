<div class="form form-container">
  <%= form_with(model: media_unit_cost, local: true) do |form| %>
    <% if media_unit_cost.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(media_unit_cost.errors.count, "error") %> prohibited this media_unit_cost from being saved:</h2>

        <ul>
        <% media_unit_cost.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= form.label :date %><br>
      <%= form.text_field :date, include_blank: true, data:{ provide: "datepicker", 'date-format': 'yyyy-mm-dd', "date-today-highlight": true }, class: "form-control", value: Date.today %>
    </div>

    <div class="form-group">
      <%= form.label :media %>
      <%= form.text_field :media, id: :media_unit_cost_media, class: "form-control", value: "biostrate felt" %>
    </div>

    <div class="form-group">
      <%= form.label :unit_cost %>
      <%= form.text_field :unit_cost, id: :media_unit_cost_unit_cost, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= form.label :notes %>
      <%= form.text_field :notes, id: :media_unit_cost_notes, class: "form-control" %>
    </div>

    <div class="actions">
      <%= form.submit "submit", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>  