
  <div class="form-container">
    <%= form_with(model: over_grow_recipient, local: true) do |form| %>
      <% if over_grow_recipient.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(over_grow_recipient.errors.count, "error") %> prohibited this over_grow_recipient from being saved:</h2>

          <ul>
          <% over_grow_recipient.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <h3><%= form.label :email %></h3>
        <%= form.text_field :email, id: :over_grow_recipient_email, class: "form-control" %>
      </div>

      <div class="actions" align="center">
        <%= form.submit "create", class: "btn btn-primary btn-lg" %>
      </div>
    <% end %>
  </div>
